#pragma once
#include "Tree.h"
#include <string>
using namespace std;
void addElemnts(Tree & tree , int size);
double valNumber();
bool isFloatNumber(string s);
int inputNumber();
bool isNumber(string s);
